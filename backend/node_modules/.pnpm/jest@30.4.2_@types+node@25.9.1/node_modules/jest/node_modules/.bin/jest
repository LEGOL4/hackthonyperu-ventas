#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../jest-cli/bin/jest.js" "$@"
else
  exec node  "$basedir/../../../jest-cli/bin/jest.js" "$@"
fi
# cmd-shim-target=C:/Users/Leonardo/hackthonyperu-ventas/backend/node_modules/.pnpm/jest@30.4.2_@types+node@25.9.1/node_modules/jest-cli/bin/jest.js
